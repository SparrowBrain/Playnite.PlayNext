<PluginUserControl x:Class="PlayNext.Views.CreatePresetView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:PlayNext.Views"
                   mc:Ignorable="d"
                   d:DesignHeight="450" d:DesignWidth="800">
    <PluginUserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
            <Setter Property="Margin" Value="0,5,5,0" />
            <Setter Property="TextWrapping" Value="Wrap"></Setter>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="5,0,5,0"></Setter>
            <Setter Property="MinWidth" Value="100"></Setter>
        </Style>
    </PluginUserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource LOC_PlayNext_SettingsPresetName}"></TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}"></TextBox>

        <StackPanel Grid.Column="1" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="{Binding CreateCommand}" Content="{DynamicResource LOC_PlayNextGenericOk}"></Button>
            <Button Command="{Binding CancelCommand}" Content="{DynamicResource LOC_PlayNextGenericCancel}"></Button>
        </StackPanel>
    </Grid>
</PluginUserControl>